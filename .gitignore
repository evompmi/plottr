.DS_Store
node_modules/
.claude/
# Reviews — local-only commissioned reviews, not part of the public history.
docs/reviews/
# Same policy applies to the older root-level review drafts that predate
# the docs/reviews/ convention. Pinned by exact filename rather than a
# wildcard so an unrelated future file (e.g. code-review-checklist.md)
# doesn't get silently ignored.
/1.2.0_harsh_review.md
/1.2.0_harsh_review_revisit.md
/plottr_harsh_review_OPUS.md
# Playwright e2e
test-results/
playwright-report/
.playwright/
playwright/.cache/
# Stryker mutation-test artefacts (local-only — large HTML reports,
# sandbox dirs, incremental cache).
reports/
.stryker-tmp/
stryker-incremental.json
# Per-tool helpers compiled to CJS for Stryker-visible require()
# (tests/helpers/scatter-loader.js → tests/.tmp/scatter-helpers.cjs etc.).
tests/.tmp/
