.DS_Store
node_modules/
.claude/
# Reviews — local-only commissioned reviews, not part of the public history.
docs/reviews/
# Playwright e2e
test-results/
playwright-report/
.playwright/
playwright/.cache/
# Stryker mutation-test artefacts (local-only — large HTML reports,
# sandbox dirs, incremental cache).
reports/
.stryker-tmp/
stryker-incremental.json
# Per-tool helpers compiled to CJS for Stryker-visible require()
# (tests/helpers/scatter-loader.js → tests/.tmp/scatter-helpers.cjs etc.).
tests/.tmp/
