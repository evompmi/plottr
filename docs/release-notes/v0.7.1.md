# Plöttr v0.7.1 — release notes

_Released 2026-04-13._

A tiny follow-up to v0.7.0: keep the StatsTile component-test
assertions in sync with the fragment-return refactor, and reformat
files flagged by `format:check` in CI.

## Fixed

- **StatsTile test assertions** — updated from `el.type === "div"` to `"Fragment"` after the component switched to returning a fragment.
- **Prettier formatting** — reformatted files flagged by `format:check` in CI.
