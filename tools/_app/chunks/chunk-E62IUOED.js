import{isNumericValue,tinv,toNumericValue}from"./chunk-ERA745UF.js";function computeStats(arr){let n=arr.length;if(n===0)return null;let mean=arr.reduce((a,b)=>a+b,0)/n,variance=arr.reduce((s,v)=>s+(v-mean)**2,0)/(n>1?n-1:1),sd=Math.sqrt(variance),sem=n>1?sd/Math.sqrt(n):0,ci95=n>1?tinv(.975,n-1)*sem:0,sorted=[...arr].sort((a,b)=>a-b),min=sorted[0],max=sorted[n-1],median=n%2===0?(sorted[n/2-1]+sorted[n/2])/2:sorted[Math.floor(n/2)];return{mean,sd,sem,ci95,n,min,max,median}}function quartiles(arr){let s=[...arr].sort((a,b)=>a-b),n=s.length;if(n===0)return null;let q=p=>{let i=p*(n-1),lo=Math.floor(i),hi=Math.min(Math.ceil(i),n-1);return lo===hi?s[lo]:s[lo]*(hi-i)+s[hi]*(i-lo)},q1=q(.25),med=q(.5),q3=q(.75),iqr=q3-q1;return{min:s[0],max:s[n-1],q1,med,q3,iqr,wLo:Math.min(s.find(v=>v>=q1-1.5*iqr)??s[0],q1),wHi:Math.max([...s].reverse().find(v=>v<=q3+1.5*iqr)??s[n-1],q3),n}}function kde(values,nPoints=50){let n=values.length;if(n===0)return[];let sorted=[...values].sort((a,b)=>a-b),min=sorted[0],max=sorted[n-1],iqr=n>=4?sorted[Math.floor(n*.75)]-sorted[Math.floor(n*.25)]:max-min,mean=values.reduce((a,b)=>a+b,0)/n,variance=values.reduce((s,v)=>s+(v-mean)**2,0)/n,std=Math.sqrt(variance),bw=1.06*Math.min(std,(iqr||1)/1.34)*n**-.2||1,pad=bw*2,lo=min-pad,step=(max+pad-lo)/(nPoints-1),pts=[];for(let i=0;i<nPoints;i++){let x=lo+i*step,density=0;for(let j=0;j<n;j++){let z=(x-values[j])/bw;density+=Math.exp(-.5*z*z)}density/=n*bw*Math.sqrt(2*Math.PI),pts.push({x,d:density})}return pts}function computeGroupStats(groups){return Object.entries(groups).map(([name,vals])=>{let nums=vals.filter(v=>v!==""&&isNumericValue(v)).map(toNumericValue),stats=computeStats(nums);return stats?{name,...stats}:{name,n:0,mean:null,sd:null,sem:null,min:null,max:null,median:null}})}export{computeStats,quartiles,kde,computeGroupStats};
//# sourceMappingURL=chunk-E62IUOED.js.map
