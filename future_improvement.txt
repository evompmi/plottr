  ---
  High Priority — DONE (2026-04-07)

  1. Inline functions/objects in render paths (all .jsx files)
     DONE — Extracted complex inline handlers (onToggleGroup, handleColorByChange, handleGroupNameChange,
     handleColorChange, handleNameChange, handleFilterToggle) from JSX into named functions at the component
     level in bargraph.jsx, boxplot.jsx, and scatter.jsx.
     Note: Simple one-liner handlers on native DOM elements (e.g. e => updVis({key: e.target.value})) were
     intentionally left inline — useCallback on native elements does not prevent re-renders unless child
     components are wrapped in React.memo, which is not the case here.

  2. Missing bounds/null guards
     DONE — quartiles() interpolation index clamped with Math.min(Math.ceil(i), n-1). Bounds checks added
     to groupsFromLong (bargraph.jsx) and boxplotGroups/facetedData builders (boxplot.jsx) for column index
     access. computeStats and quartiles already returned null for empty arrays (no change needed).
     4 new tests added.

  3. Floating-point edge cases in shared.js
     DONE — getPointColors() t clamped with Math.min(1, ...). makeTicks() now double-checks rounded ticks
     against max to prevent stray ticks from toPrecision rounding. 2 new tests added.

  ---
  Medium Priority — DONE (2026-04-07)

  4. var vs const/let inconsistency
     DONE — Converted all var declarations to const/let in shared.js (toolIcon, downloadPng) and
     shared-components.js (throughout). Identified 4 cases requiring let (accumulators, conditionally
     assigned variables) vs const for the rest.

  5. O(n^2) legend block positioning (shared-components.js:122-135)
     DONE — Replaced per-block slice+reduce with pre-computed blockOffsets array in a single pass.
     Also modernized Math.max.apply(null, arr) to Math.max(...arr) throughout.

  6. Stale callback refs in faceted charts (bargraph.jsx, boxplot.jsx, aequorin.jsx)
     DONE — Extracted facet items into React.memo wrapper components (FacetBarItem, FacetBoxplotItem,
     FacetChartItem) with useRef + useEffect for stable ref registration/cleanup, eliminating inline
     ref={el => ...} in .map() loops.

  7. SVG accessibility
     DONE — Added role="img", aria-label, <title>, and <desc> to all root <svg> elements in
     bargraph.jsx, boxplot.jsx, scatter.jsx, aequorin.jsx (Chart + InsetBarplot), and venn.jsx.
     Added aria-label on data-bearing groups (bar groups, box groups, data point groups, Venn circles).
     Decorative SVGs marked with aria-hidden="true".

  ---
  Low Priority

  8. Duplicated margin constants — bargraph.jsx, scatter.jsx, boxplot.jsx each define their own margin
  objects. Could consolidate in shared.js, but each tool has different bottom-margin logic (angle-dependent),
  so a shared constant would need per-tool overrides anyway. Marginal benefit.

  9. Outdated Math.max.apply(null, arr) pattern (shared-components.js) — DONE (addressed as part of #5).

  10. Color input regex (shared-components.js:7) — Only accepts 6-digit hex (#aabbcc), rejects valid
  3-digit hex (#abc). HTML5 color pickers always produce 6-digit hex, so this is only a problem if users
  type values manually.

  11. No minification in build — esbuild scripts don't pass --minify, so output is larger than necessary
  for production. Add --minify to the build script in package.json for smaller deployments.

  ---
  Not Issues

  - The plain JS constraint on shared.js/shared-components.js is documented and intentional.
  - The seeded PRNG (Park & Miller LCG) is correct for jitter use.
  - No server-side code means no injection/auth risks — data stays client-side.
  - Simple inline handlers on native elements (e => updVis({...})) are not a performance concern —
    useCallback only helps when callbacks are passed to React.memo-wrapped children.
